# API Reference

The API reference below is built inside the same GitBook space.

Guide pages stay at the top level of the sidebar, and the endpoint reference lives under this `API Reference` section.
